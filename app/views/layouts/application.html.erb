<!DOCTYPE html>
<html>
  <head>
    <title>RollAD6</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="relative min-h-screen">
  <div class="bg-image absolute inset-0 z-0" style="background-image: url(<%= asset_path('dndImage.jpeg') %>);"></div>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="relative z-10">
      <header class="bg-blue-600 text-white p-2">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
          <h1 class="font-bold p-5">The Dragons Den</h1>
          <nav>
            <ul class="flex space-x-4">
              <% if user_signed_in? %>
                <li><%= link_to "Home", home_path, class: "hover:underline" %></li> <li></li>|
                <li><%= link_to "Edit Profile", edit_user_registration_path, class: "hover:underline" %></li> <li></li>| 
                <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "hover:underline" %></li> <li></li>
              <% else %>
                <li><%= link_to "Home", home_path, class: "hover:underline" %></li> <li></li>|
                <li><%= link_to "Sign Up", new_user_registration_path, class: "hover:underline" %></li> <li></li>| 
                <li><%= link_to "Login", new_user_session_path, class: "hover:underline" %></li> <li></li>
              <% end %>
            </ul>
          </nav>
        </div>
      </header>
      <%= yield %>
    </div>
  </body>
</html>
